# 六爻 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个时髦的六爻（周易金钱卦）算命静态网页，托管于 GitHub Pages：输入所问 → 摇卦 → 出卦（卦象 + 周易原文）→ 生成分享卡片。

**Architecture:** 纯前端 React 单页应用。领域逻辑（起卦/查卦/解卦）为零依赖纯函数，与 React 解耦、可独立测试；解卦经由一个 async `interpret()` 接缝，一期返回本地周易原文，二期可换成调 serverless 的 AI。线性状态机 `input → casting → result`。

**Tech Stack:** Vite · React · TypeScript · Tailwind CSS · Framer Motion · modern-screenshot（DOM→PNG）· Vitest + @testing-library/react（单元/集成）· Playwright（E2E）· GitHub Actions → GitHub Pages。

参考 spec：`docs/superpowers/specs/2026-06-16-liuyao-fortune-design.md`

---

## File Structure

```
src/
  domain/
    types.ts            # Line / Hexagram / 结果类型
    lines.ts            # 三铜钱之和 → 爻（阴阳/动静）
    random.ts           # RandomSource 接口 + crypto 实现 + mock 工厂
    casting.ts          # 抛钱 → 六掷 → 本卦 → 动爻 → 变卦
    hexagram-key.ts     # 六爻 → 二进制键
    hexagram-lookup.ts  # 键 → 卦数据；构建查表
    interpret.ts        # 解卦接缝（async；一期=本地原文）
    reading.ts          # buildReading：串起 cast→lookup→组装
  data/
    hexagrams.ts        # 六十四卦数据集（卦名/卦象/卦辞/爻辞/世应）
  components/
    Hexagram.tsx        # 纯展示：六爻 + 动爻/世应标记
    QuestionInput.tsx   # 所问输入（必填校验）
    CoinToss.tsx        # 单次三铜钱抛掷动画
    CastingStage.tsx    # 摇卦编排：六掷、逐爻成卦
    ResultView.tsx      # 出卦：本卦/变卦 + 卦名 + 卦辞 + 动爻爻辞
    ShareCard.tsx       # 分享卡（离屏渲染）
  hooks/
    useCasting.ts       # 状态机 input→casting→result
    useShareImage.ts    # DOM→PNG + 保存/分享
  styles/
    theme.css           # 墨金 design tokens（CSS 变量）
    fonts.css           # 子集化宋体 webfont @font-face
  App.tsx
  main.tsx
tests/
  e2e/divination.spec.ts
```

每个文件单一职责；领域层不 import React。

---

## Phase 0 — 脚手架与工具链

### Task 0: 初始化项目

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.js`, `vitest.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/styles/theme.css`

- [ ] **Step 1: 用 Vite 脚手架创建 React+TS 项目（在当前目录）**

Run:
```bash
npm create vite@latest . -- --template react-ts
npm install
```
若提示目录非空，选择忽略已有文件（保留 `LICENSE`、`.gitignore`、`docs/`）。

- [ ] **Step 2: 安装依赖**

Run:
```bash
npm install framer-motion modern-screenshot
npm install -D tailwindcss postcss autoprefixer vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @vitejs/plugin-react @playwright/test
npx tailwindcss init -p
```

- [ ] **Step 3: 配置 Vite base 与测试**

Create `vite.config.ts`:
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// GitHub Project Pages 子路径；部署仓库名若变动，改这里
export default defineConfig({
  base: '/liuyao/',
  plugins: [react()],
})
```

Create `vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
    exclude: ['**/node_modules/**', 'tests/e2e/**'],
    coverage: { provider: 'v8', thresholds: { lines: 80, functions: 80, branches: 80, statements: 80 } },
  },
})
```

Create `src/test-setup.ts`:
```ts
import '@testing-library/jest-dom'
```

- [ ] **Step 4: 配置 Tailwind 主题（墨金 tokens）**

Replace `tailwind.config.ts`:
```ts
import type { Config } from 'tailwindcss'

export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        ink: '#080604',
        'ink-2': '#15110b',
        gold: '#e6bf72',
        'gold-bright': '#f0d68a',
        cinnabar: '#ff6a52',
        seal: '#b8312a',
        paper: '#f0e6cf',
      },
      fontFamily: { serif: ["'Songti SC'", "'STSong'", 'serif'] },
    },
  },
  plugins: [],
} satisfies Config
```

Create `src/styles/theme.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root { color-scheme: dark; }
body {
  margin: 0;
  background: radial-gradient(circle at 50% 28%, #15110b, #080604);
  color: #f0e6cf;
  min-height: 100vh;
}
.glow-gold { filter: drop-shadow(0 0 4px rgba(230, 191, 114, 0.4)); }
.text-glow-gold { text-shadow: 0 0 10px rgba(240, 214, 138, 0.4); }
```

Import it in `src/main.tsx` (replace any default CSS import with `import './styles/theme.css'`).

- [ ] **Step 5: 加测试脚本**

In `package.json` `"scripts"`, ensure:
```json
"test": "vitest run",
"test:watch": "vitest",
"e2e": "playwright test",
"build": "tsc -b && vite build",
"dev": "vite"
```

- [ ] **Step 6: 冒烟验证**

Run: `npm run test`
Expected: 通过（0 个测试也算通过）或提示无测试文件；无配置报错。
Run: `npm run build`
Expected: 构建成功，产物在 `dist/`。

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "chore: 脚手架 Vite+React+TS+Tailwind+Vitest"
```

---

## Phase 1 — 领域逻辑（纯函数，TDD）

### Task 1: 类型与「和→爻」映射

**Files:**
- Create: `src/domain/types.ts`, `src/domain/lines.ts`
- Test: `src/domain/lines.test.ts`

- [ ] **Step 1: 写类型**

Create `src/domain/types.ts`:
```ts
export type Yinyang = 'yin' | 'yang'

/** 一爻：阴阳 + 是否动爻（老阴/老阳为动） */
export interface Line {
  yinyang: Yinyang
  moving: boolean
}

/** 一卦：六爻，index 0 = 初爻(最下)，index 5 = 上爻(最上) */
export type Hexagram = [Line, Line, Line, Line, Line, Line]
```

- [ ] **Step 2: 写失败测试**

Create `src/domain/lines.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { lineFromSum } from './lines'

describe('lineFromSum', () => {
  it('6 → 老阴（阴，动）', () => expect(lineFromSum(6)).toEqual({ yinyang: 'yin', moving: true }))
  it('7 → 少阳（阳，静）', () => expect(lineFromSum(7)).toEqual({ yinyang: 'yang', moving: false }))
  it('8 → 少阴（阴，静）', () => expect(lineFromSum(8)).toEqual({ yinyang: 'yin', moving: false }))
  it('9 → 老阳（阳，动）', () => expect(lineFromSum(9)).toEqual({ yinyang: 'yang', moving: true }))
  it('非法和抛错', () => expect(() => lineFromSum(5)).toThrow())
})
```

- [ ] **Step 3: 运行，确认失败**

Run: `npm run test -- lines`
Expected: FAIL（`lineFromSum` 未定义）

- [ ] **Step 4: 实现**

Create `src/domain/lines.ts`:
```ts
import { Line } from './types'

/** 三铜钱之和（字=2，背=3）映射到爻。和的奇偶定阴阳；6/9（全同）为动爻。 */
export function lineFromSum(sum: number): Line {
  switch (sum) {
    case 6: return { yinyang: 'yin', moving: true }    // 老阴
    case 7: return { yinyang: 'yang', moving: false }  // 少阳
    case 8: return { yinyang: 'yin', moving: false }   // 少阴
    case 9: return { yinyang: 'yang', moving: true }   // 老阳
    default: throw new Error(`无效的铜钱之和: ${sum}（应为 6–9）`)
  }
}
```

- [ ] **Step 5: 运行，确认通过**

Run: `npm run test -- lines`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/domain/types.ts src/domain/lines.ts src/domain/lines.test.ts
git commit -m "feat: 爻类型与三铜钱之和→爻映射"
```

---

### Task 2: 随机源（可注入）

**Files:**
- Create: `src/domain/random.ts`
- Test: `src/domain/random.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/domain/random.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { sequenceRandom, cryptoRandom } from './random'

describe('sequenceRandom（测试用 mock）', () => {
  it('按给定序列返回，循环复用', () => {
    const rng = sequenceRandom([0, 1, 0])
    expect(rng.nextInt(2)).toBe(0)
    expect(rng.nextInt(2)).toBe(1)
    expect(rng.nextInt(2)).toBe(0)
    expect(rng.nextInt(2)).toBe(0) // 循环回头
  })
})

describe('cryptoRandom', () => {
  it('nextInt 落在 [0, max)', () => {
    for (let i = 0; i < 200; i++) {
      const v = cryptoRandom.nextInt(2)
      expect(v).toBeGreaterThanOrEqual(0)
      expect(v).toBeLessThan(2)
    }
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- random`
Expected: FAIL（模块未定义）

- [ ] **Step 3: 实现**

Create `src/domain/random.ts`:
```ts
export interface RandomSource {
  /** 返回 [0, max) 的整数 */
  nextInt(max: number): number
}

/** 生产用：crypto，拒绝采样消除取模偏差 */
export const cryptoRandom: RandomSource = {
  nextInt(max: number): number {
    if (max <= 0) throw new Error('max 必须为正')
    const limit = Math.floor(0xffffffff / max) * max
    const buf = new Uint32Array(1)
    let x = 0
    do {
      crypto.getRandomValues(buf)
      x = buf[0]
    } while (x >= limit)
    return x % max
  },
}

/** 测试用：按固定序列返回，循环复用 */
export function sequenceRandom(seq: number[]): RandomSource {
  if (seq.length === 0) throw new Error('序列不能为空')
  let i = 0
  return {
    nextInt(_max: number): number {
      const v = seq[i % seq.length]
      i++
      return v
    },
  }
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- random`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/domain/random.ts src/domain/random.test.ts
git commit -m "feat: 可注入随机源（crypto + 测试 mock）"
```

---

### Task 3: 起卦与变卦

**Files:**
- Create: `src/domain/casting.ts`
- Test: `src/domain/casting.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/domain/casting.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { sequenceRandom } from './random'
import { tossCoin, tossThrow, castHexagram, changedHexagram, movingLineIndexes } from './casting'
import { Hexagram } from './types'

describe('tossCoin', () => {
  it('0→字(2)，1→背(3)', () => {
    expect(tossCoin(sequenceRandom([0]))).toBe(2)
    expect(tossCoin(sequenceRandom([1]))).toBe(3)
  })
})

describe('tossThrow', () => {
  it('三枚全字 → 6（老阴）', () => expect(tossThrow(sequenceRandom([0, 0, 0]))).toBe(6))
  it('两字一背 → 7', () => expect(tossThrow(sequenceRandom([0, 0, 1]))).toBe(7))
  it('三枚全背 → 9（老阳）', () => expect(tossThrow(sequenceRandom([1, 1, 1]))).toBe(9))
})

describe('castHexagram', () => {
  it('自下而上六爻；全字六掷 = 六个老阴', () => {
    const h = castHexagram(sequenceRandom([0, 0, 0]))
    expect(h).toHaveLength(6)
    h.forEach((l) => expect(l).toEqual({ yinyang: 'yin', moving: true }))
  })
})

describe('changedHexagram', () => {
  it('无动爻 → null', () => {
    const stable: Hexagram = Array.from({ length: 6 }, () => ({ yinyang: 'yang', moving: false })) as Hexagram
    expect(changedHexagram(stable)).toBeNull()
  })
  it('动爻翻面，且变卦各爻 moving=false', () => {
    const h: Hexagram = [
      { yinyang: 'yin', moving: true },   // 动 → 变阳
      { yinyang: 'yang', moving: false },
      { yinyang: 'yang', moving: true },  // 动 → 变阴
      { yinyang: 'yin', moving: false },
      { yinyang: 'yang', moving: false },
      { yinyang: 'yin', moving: false },
    ]
    const c = changedHexagram(h)!
    expect(c[0]).toEqual({ yinyang: 'yang', moving: false })
    expect(c[2]).toEqual({ yinyang: 'yin', moving: false })
    expect(c[1]).toEqual({ yinyang: 'yang', moving: false })
  })
})

describe('movingLineIndexes', () => {
  it('返回动爻下标', () => {
    const h: Hexagram = [
      { yinyang: 'yin', moving: true },
      { yinyang: 'yang', moving: false },
      { yinyang: 'yang', moving: true },
      { yinyang: 'yin', moving: false },
      { yinyang: 'yang', moving: false },
      { yinyang: 'yin', moving: false },
    ]
    expect(movingLineIndexes(h)).toEqual([0, 2])
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- casting`
Expected: FAIL（模块未定义）

- [ ] **Step 3: 实现**

Create `src/domain/casting.ts`:
```ts
import { RandomSource } from './random'
import { Line, Hexagram, Yinyang } from './types'
import { lineFromSum } from './lines'

/** 一枚铜钱：字=2 或 背=3，各 1/2 */
export function tossCoin(rng: RandomSource): 2 | 3 {
  return rng.nextInt(2) === 0 ? 2 : 3
}

/** 一掷 = 三枚铜钱之和（6–9） */
export function tossThrow(rng: RandomSource): number {
  return tossCoin(rng) + tossCoin(rng) + tossCoin(rng)
}

/** 摇六次，自下而上成本卦 */
export function castHexagram(rng: RandomSource): Hexagram {
  const lines: Line[] = []
  for (let i = 0; i < 6; i++) lines.push(lineFromSum(tossThrow(rng)))
  return lines as Hexagram
}

const flip = (y: Yinyang): Yinyang => (y === 'yin' ? 'yang' : 'yin')

/** 变卦：动爻翻面；无动爻返回 null */
export function changedHexagram(h: Hexagram): Hexagram | null {
  if (!h.some((l) => l.moving)) return null
  return h.map((l) => ({
    yinyang: l.moving ? flip(l.yinyang) : l.yinyang,
    moving: false,
  })) as Hexagram
}

/** 动爻下标（0=初爻） */
export function movingLineIndexes(h: Hexagram): number[] {
  return h.reduce<number[]>((acc, l, i) => (l.moving ? [...acc, i] : acc), [])
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- casting`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/domain/casting.ts src/domain/casting.test.ts
git commit -m "feat: 起卦（六掷成卦）与变卦推导"
```

---

### Task 4: 六爻 → 二进制键

**Files:**
- Create: `src/domain/hexagram-key.ts`
- Test: `src/domain/hexagram-key.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/domain/hexagram-key.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { hexagramKey } from './hexagram-key'

describe('hexagramKey', () => {
  it('自下而上，阳=1 阴=0', () => {
    // 地天泰：下乾(三阳) 上坤(三阴) → 初..上 = 1,1,1,0,0,0
    const tai = [
      { yinyang: 'yang' }, { yinyang: 'yang' }, { yinyang: 'yang' },
      { yinyang: 'yin' }, { yinyang: 'yin' }, { yinyang: 'yin' },
    ] as const
    expect(hexagramKey(tai)).toBe('111000')
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- hexagram-key`
Expected: FAIL

- [ ] **Step 3: 实现**

Create `src/domain/hexagram-key.ts`:
```ts
import { Yinyang } from './types'

/** 六爻（自下而上）→ 6 位二进制键，阳=1 阴=0 */
export function hexagramKey(lines: readonly { yinyang: Yinyang }[]): string {
  if (lines.length !== 6) throw new Error('必须为六爻')
  return lines.map((l) => (l.yinyang === 'yang' ? '1' : '0')).join('')
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- hexagram-key`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/domain/hexagram-key.ts src/domain/hexagram-key.test.ts
git commit -m "feat: 六爻→二进制键"
```

---

## Phase 2 — 六十四卦数据集

### Task 5: 数据集结构 + 完整性测试（先写测试，再填数据）

**Files:**
- Create: `src/data/hexagrams.ts`, `src/domain/hexagram-data.ts`（类型）
- Test: `src/data/hexagrams.test.ts`

> **说明：** 卦名/卦辞/爻辞为公版《周易》文字，世应位依八宫固定。这是「由测试守护的数据录入」任务：先写完整性测试（必失败），再逐卦填入直到全绿。数据来源：公版《周易》卦爻辞 + 八宫世应表。

- [ ] **Step 1: 写数据类型**

Create `src/domain/hexagram-data.ts`:
```ts
export interface HexagramData {
  order: number        // King Wen 卦序 1–64
  name: string         // 卦名，如「地天泰」
  symbol: string       // 卦符，如 ䷊
  upper: string        // 上卦（八经卦名）
  lower: string        // 下卦
  lines: boolean[]     // 六爻自下而上，true=阳
  judgment: string     // 卦辞
  lineTexts: string[]  // 六条爻辞，自下而上，长度 6
  shiYao: number       // 世爻位 1–6
  yingYao: number      // 应爻位 1–6
}
```

- [ ] **Step 2: 写失败的完整性测试**

Create `src/data/hexagrams.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { hexagrams } from './hexagrams'
import { hexagramKey } from '../domain/hexagram-key'

describe('六十四卦数据集完整性', () => {
  it('恰好 64 卦', () => expect(hexagrams).toHaveLength(64))

  it('卦序 1–64 无重复', () => {
    const orders = hexagrams.map((h) => h.order).sort((a, b) => a - b)
    expect(orders).toEqual(Array.from({ length: 64 }, (_, i) => i + 1))
  })

  it('每卦六爻、六爻辞，世应位合法', () => {
    for (const h of hexagrams) {
      expect(h.lines, h.name).toHaveLength(6)
      expect(h.lineTexts, h.name).toHaveLength(6)
      expect(h.judgment.length, h.name).toBeGreaterThan(0)
      expect(h.shiYao, h.name).toBeGreaterThanOrEqual(1)
      expect(h.shiYao, h.name).toBeLessThanOrEqual(6)
      expect(h.yingYao, h.name).toBeGreaterThanOrEqual(1)
      expect(h.yingYao, h.name).toBeLessThanOrEqual(6)
    }
  })

  it('二进制键唯一且覆盖全部 64 种组合', () => {
    const keys = new Set(hexagrams.map((h) => hexagramKey(h.lines.map((b) => ({ yinyang: b ? 'yang' : 'yin' })))))
    expect(keys.size).toBe(64)
  })

  it('已知卦抽查：乾为天（六阳，世上爻应三爻）', () => {
    const qian = hexagrams.find((h) => h.name.includes('乾为天'))!
    expect(qian.lines).toEqual([true, true, true, true, true, true])
    expect(qian.shiYao).toBe(6)
    expect(qian.yingYao).toBe(3)
  })

  it('已知卦抽查：地天泰（下乾上坤 = 111000）', () => {
    const tai = hexagrams.find((h) => h.name.includes('泰'))!
    expect(tai.lines).toEqual([true, true, true, false, false, false])
  })
})
```

- [ ] **Step 3: 运行，确认失败**

Run: `npm run test -- hexagrams`
Expected: FAIL（`hexagrams` 未定义 / 数量不足）

- [ ] **Step 4: 建文件骨架 + 前两卦样例**

Create `src/data/hexagrams.ts`（先放结构与样例，Step 5 填满）:
```ts
import { HexagramData } from '../domain/hexagram-data'

// 数据来源：公版《周易》卦爻辞 + 八宫世应表。lines 自下而上，true=阳。
export const hexagrams: HexagramData[] = [
  {
    order: 1,
    name: '乾为天',
    symbol: '䷀',
    upper: '乾',
    lower: '乾',
    lines: [true, true, true, true, true, true],
    judgment: '乾：元，亨，利，贞。',
    lineTexts: [
      '初九：潜龙勿用。',
      '九二：见龙在田，利见大人。',
      '九三：君子终日乾乾，夕惕若厉，无咎。',
      '九四：或跃在渊，无咎。',
      '九五：飞龙在天，利见大人。',
      '上九：亢龙有悔。',
    ],
    shiYao: 6,
    yingYao: 3,
  },
  {
    order: 11,
    name: '地天泰',
    symbol: '䷊',
    upper: '坤',
    lower: '乾',
    lines: [true, true, true, false, false, false],
    judgment: '泰：小往大来，吉亨。',
    lineTexts: [
      '初九：拔茅茹，以其汇，征吉。',
      '九二：包荒，用冯河，不遐遗，朋亡，得尚于中行。',
      '九三：无平不陂，无往不复，艰贞无咎，勿恤其孚，于食有福。',
      '六四：翩翩不富，以其邻，不戒以孚。',
      '六五：帝乙归妹，以祉元吉。',
      '上六：城复于隍，勿用师，自邑告命，贞吝。',
    ],
    shiYao: 3,
    yingYao: 6,
  },
  // …… 其余 62 卦在 Step 5 补齐
]
```

- [ ] **Step 5: 补齐全部 64 卦**

按《周易》King Wen 卦序 1–64 录入剩余 62 卦，每卦字段同上。要点：
- `lines` 自下而上：下卦为初/二/三爻，上卦为四/五/上爻；阳爻 `true`，阴爻 `false`。
- `judgment`、`lineTexts` 用公版原文；`lineTexts` 必须 6 条、自下而上。
- `shiYao`/`yingYao` 依八宫世应表填写（世应相隔三位）。
- 全程以 Step 2 的测试为守护，填一批就跑一次。

- [ ] **Step 6: 运行直到全绿**

Run: `npm run test -- hexagrams`
Expected: PASS（64 卦齐全、键唯一覆盖、抽查通过）

- [ ] **Step 7: Commit**

```bash
git add src/domain/hexagram-data.ts src/data/hexagrams.ts src/data/hexagrams.test.ts
git commit -m "feat: 六十四卦数据集（卦辞/爻辞/世应）+ 完整性测试"
```

---

### Task 6: 查卦

**Files:**
- Create: `src/domain/hexagram-lookup.ts`
- Test: `src/domain/hexagram-lookup.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/domain/hexagram-lookup.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { lookupHexagram } from './hexagram-lookup'
import { Hexagram } from './types'

const linesOf = (bits: string): Hexagram =>
  bits.split('').map((b) => ({ yinyang: b === '1' ? 'yang' : 'yin', moving: false })) as Hexagram

describe('lookupHexagram', () => {
  it('111111 → 乾为天', () => expect(lookupHexagram(linesOf('111111')).name).toContain('乾为天'))
  it('111000 → 泰', () => expect(lookupHexagram(linesOf('111000')).name).toContain('泰'))
  it('全部 64 键均可命中', () => {
    for (let n = 0; n < 64; n++) {
      const bits = n.toString(2).padStart(6, '0')
      expect(() => lookupHexagram(linesOf(bits))).not.toThrow()
    }
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- hexagram-lookup`
Expected: FAIL

- [ ] **Step 3: 实现**

Create `src/domain/hexagram-lookup.ts`:
```ts
import { hexagrams } from '../data/hexagrams'
import { HexagramData } from './hexagram-data'
import { hexagramKey } from './hexagram-key'
import { Yinyang } from './types'

const byKey = new Map<string, HexagramData>(
  hexagrams.map((h) => [hexagramKey(h.lines.map((b) => ({ yinyang: b ? 'yang' : 'yin' }))), h]),
)

export function lookupHexagram(lines: readonly { yinyang: Yinyang }[]): HexagramData {
  const data = byKey.get(hexagramKey(lines))
  if (!data) throw new Error(`查无此卦: ${hexagramKey(lines)}`)
  return data
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- hexagram-lookup`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/domain/hexagram-lookup.ts src/domain/hexagram-lookup.test.ts
git commit -m "feat: 查卦（二进制键→卦数据）"
```

---

### Task 7: 组装 reading + 解卦接缝

**Files:**
- Create: `src/domain/reading.ts`, `src/domain/interpret.ts`
- Test: `src/domain/reading.test.ts`, `src/domain/interpret.test.ts`

- [ ] **Step 1: 写失败测试（reading）**

Create `src/domain/reading.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { sequenceRandom } from './random'
import { buildReading } from './reading'

describe('buildReading', () => {
  it('全字六掷 → 本卦乾为天，六动爻，变卦坤为地', () => {
    const r = buildReading('我该换工作吗？', sequenceRandom([0, 0, 0]))
    expect(r.question).toBe('我该换工作吗？')
    expect(r.primary.data.name).toContain('乾为天')
    expect(r.movingIndexes).toEqual([0, 1, 2, 3, 4, 5])
    expect(r.changed?.data.name).toContain('坤为地')
  })

  it('全字背交替模拟无动爻时 changed 为 null', () => {
    // 7(两字一背)与8(一字两背)均为静爻：序列 [0,0,1] 重复 → 每掷=7(少阳)
    const r = buildReading('问', sequenceRandom([0, 0, 1]))
    expect(r.movingIndexes).toEqual([])
    expect(r.changed).toBeNull()
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- reading`
Expected: FAIL

- [ ] **Step 3: 实现 reading**

Create `src/domain/reading.ts`:
```ts
import { RandomSource } from './random'
import { Hexagram } from './types'
import { castHexagram, changedHexagram, movingLineIndexes } from './casting'
import { lookupHexagram } from './hexagram-lookup'
import { HexagramData } from './hexagram-data'

export interface HexagramResult {
  lines: Hexagram
  data: HexagramData
}

export interface CastReading {
  question: string
  primary: HexagramResult
  changed: HexagramResult | null
  movingIndexes: number[]
}

export function buildReading(question: string, rng: RandomSource): CastReading {
  const primaryLines = castHexagram(rng)
  const changedLines = changedHexagram(primaryLines)
  return {
    question,
    primary: { lines: primaryLines, data: lookupHexagram(primaryLines) },
    changed: changedLines ? { lines: changedLines, data: lookupHexagram(changedLines) } : null,
    movingIndexes: movingLineIndexes(primaryLines),
  }
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- reading`
Expected: PASS

- [ ] **Step 5: 写失败测试（interpret）**

Create `src/domain/interpret.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { sequenceRandom } from './random'
import { buildReading } from './reading'
import { interpret } from './interpret'

describe('interpret（一期：本地原文）', () => {
  it('有动爻 → 展示对应动爻爻辞', async () => {
    const r = buildReading('问', sequenceRandom([0, 0, 0])) // 六动爻
    const out = await interpret(r)
    expect(out.primaryName).toContain('乾为天')
    expect(out.changedName).toContain('坤为地')
    expect(out.judgment).toContain('元，亨，利，贞')
    expect(out.movingLineTexts).toHaveLength(6)
    expect(out.movingLineTexts[0]).toEqual({ index: 0, text: expect.stringContaining('潜龙勿用') })
  })

  it('无动爻 → 看本卦卦辞，movingLineTexts 为空', async () => {
    const r = buildReading('问', sequenceRandom([0, 0, 1])) // 每掷=7
    const out = await interpret(r)
    expect(out.movingLineTexts).toEqual([])
    expect(out.judgment.length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 6: 运行，确认失败**

Run: `npm run test -- interpret`
Expected: FAIL

- [ ] **Step 7: 实现 interpret（async 接缝）**

Create `src/domain/interpret.ts`:
```ts
import { CastReading } from './reading'

export interface MovingLineText {
  index: number // 0=初爻
  text: string
}

export interface Interpretation {
  question: string
  primaryName: string
  changedName: string | null
  judgment: string
  movingLineTexts: MovingLineText[]
}

/**
 * 解卦接缝。一期：本地组装周易原文（取爻辞规则：有动爻列出各动爻爻辞，无动爻看卦辞）。
 * 二期：替换为 fetch 调 serverless 的 AI 解读，签名不变。
 */
export async function interpret(reading: CastReading): Promise<Interpretation> {
  const { question, primary, changed, movingIndexes } = reading
  return {
    question,
    primaryName: primary.data.name,
    changedName: changed?.data.name ?? null,
    judgment: primary.data.judgment,
    movingLineTexts: movingIndexes.map((i) => ({ index: i, text: primary.data.lineTexts[i] })),
  }
}
```

- [ ] **Step 8: 运行，确认通过**

Run: `npm run test -- interpret`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add src/domain/reading.ts src/domain/interpret.ts src/domain/reading.test.ts src/domain/interpret.test.ts
git commit -m "feat: 组装 reading 与解卦接缝（async interpret）"
```

---

## Phase 3 — UI 组件（TDD）

### Task 8: Hexagram 展示组件

**Files:**
- Create: `src/components/Hexagram.tsx`
- Test: `src/components/Hexagram.test.tsx`

- [ ] **Step 1: 写失败测试**

Create `src/components/Hexagram.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { Hexagram } from './Hexagram'
import { Hexagram as Hex } from '../domain/types'

const tai: Hex = [
  { yinyang: 'yang', moving: false },
  { yinyang: 'yang', moving: true },
  { yinyang: 'yang', moving: false },
  { yinyang: 'yin', moving: false },
  { yinyang: 'yin', moving: false },
  { yinyang: 'yin', moving: false },
]

describe('Hexagram', () => {
  it('渲染六爻', () => {
    render(<Hexagram lines={tai} shiYao={3} yingYao={6} />)
    expect(screen.getAllByTestId('yao')).toHaveLength(6)
  })
  it('动爻有 data-moving 标记', () => {
    render(<Hexagram lines={tai} shiYao={3} yingYao={6} />)
    const moving = screen.getAllByTestId('yao').filter((el) => el.getAttribute('data-moving') === 'true')
    expect(moving).toHaveLength(1)
  })
  it('显示世应标记', () => {
    render(<Hexagram lines={tai} shiYao={3} yingYao={6} />)
    expect(screen.getByText('世')).toBeInTheDocument()
    expect(screen.getByText('应')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- Hexagram`
Expected: FAIL

- [ ] **Step 3: 实现**

Create `src/components/Hexagram.tsx`:
```tsx
import { Hexagram as Hex } from '../domain/types'

interface Props {
  lines: Hex
  shiYao: number  // 1–6
  yingYao: number // 1–6
}

/** 自上而下渲染（上爻在上），故倒序遍历；index+1 即爻位 */
export function Hexagram({ lines, shiYao, yingYao }: Props) {
  return (
    <div className="flex flex-col gap-2 items-center">
      {[...lines].map((line, i) => i).reverse().map((i) => {
        const line = lines[i]
        const pos = i + 1
        const tag = pos === shiYao ? '世' : pos === yingYao ? '应' : ''
        return (
          <div key={i} className="flex items-center gap-3">
            <div
              data-testid="yao"
              data-moving={line.moving}
              className="flex gap-2 glow-gold text-gold"
            >
              {line.yinyang === 'yang' ? (
                <span className="block h-2 w-20 rounded-sm bg-current" />
              ) : (
                <>
                  <span className="block h-2 w-8 rounded-sm bg-current" />
                  <span className="block h-2 w-8 rounded-sm bg-current" />
                </>
              )}
            </div>
            <span className="w-4 text-xs text-cinnabar">{tag}</span>
            {line.moving && <span className="h-2 w-2 rounded-full bg-cinnabar" aria-label="动爻" />}
          </div>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- Hexagram`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/components/Hexagram.tsx src/components/Hexagram.test.tsx
git commit -m "feat: Hexagram 卦象展示组件（动爻/世应标记）"
```

---

### Task 9: QuestionInput（必填校验）

**Files:**
- Create: `src/components/QuestionInput.tsx`
- Test: `src/components/QuestionInput.test.tsx`

- [ ] **Step 1: 写失败测试**

Create `src/components/QuestionInput.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { QuestionInput } from './QuestionInput'

describe('QuestionInput', () => {
  it('空输入时摇卦按钮禁用', () => {
    render(<QuestionInput onSubmit={vi.fn()} />)
    expect(screen.getByRole('button', { name: /摇卦/ })).toBeDisabled()
  })

  it('输入后启用，点击回传问题', async () => {
    const onSubmit = vi.fn()
    render(<QuestionInput onSubmit={onSubmit} />)
    await userEvent.type(screen.getByRole('textbox'), '我该换工作吗？')
    const btn = screen.getByRole('button', { name: /摇卦/ })
    expect(btn).toBeEnabled()
    await userEvent.click(btn)
    expect(onSubmit).toHaveBeenCalledWith('我该换工作吗？')
  })

  it('纯空白视为未填', async () => {
    render(<QuestionInput onSubmit={vi.fn()} />)
    await userEvent.type(screen.getByRole('textbox'), '   ')
    expect(screen.getByRole('button', { name: /摇卦/ })).toBeDisabled()
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- QuestionInput`
Expected: FAIL

- [ ] **Step 3: 实现**

Create `src/components/QuestionInput.tsx`:
```tsx
import { useState } from 'react'

interface Props {
  onSubmit: (question: string) => void
}

export function QuestionInput({ onSubmit }: Props) {
  const [value, setValue] = useState('')
  const trimmed = value.trim()
  const valid = trimmed.length > 0

  return (
    <div className="flex flex-col items-center gap-6 px-6 w-full max-w-md mx-auto">
      <h1 className="font-serif text-2xl text-gold-bright text-glow-gold">心有所问</h1>
      <label className="w-full">
        <span className="block text-xs text-paper/50 mb-2 font-serif">写下你想问的事</span>
        <textarea
          className="w-full bg-transparent border-b border-gold/40 text-paper font-serif p-2 outline-none focus:border-gold resize-none"
          rows={2}
          value={value}
          onChange={(e) => setValue(e.target.value)}
          placeholder="心诚则灵…"
        />
      </label>
      {!valid && <p className="text-xs text-cinnabar/80 font-serif">心诚则灵，先写下所问</p>}
      <button
        className="font-serif tracking-[0.3em] text-gold-bright border border-gold/60 rounded-full px-6 py-2 disabled:opacity-30 disabled:cursor-not-allowed"
        disabled={!valid}
        onClick={() => valid && onSubmit(trimmed)}
      >
        诚 心 摇 卦
      </button>
    </div>
  )
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- QuestionInput`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/components/QuestionInput.tsx src/components/QuestionInput.test.tsx
git commit -m "feat: QuestionInput（所问必填校验）"
```

---

### Task 10: CoinToss + CastingStage（摇卦编排）

**Files:**
- Create: `src/components/CoinToss.tsx`, `src/components/CastingStage.tsx`
- Test: `src/components/CastingStage.test.tsx`

> 动画帧不做断言；测试编排逻辑——六掷完成后回调一次、可「跳过」立即完成。CoinToss 为纯视觉子组件。

- [ ] **Step 1: 写失败测试**

Create `src/components/CastingStage.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { CastingStage } from './CastingStage'

describe('CastingStage', () => {
  it('点击「跳过」立即完成并回调一次', async () => {
    const onComplete = vi.fn()
    render(<CastingStage onComplete={onComplete} />)
    await userEvent.click(screen.getByRole('button', { name: /跳过/ }))
    expect(onComplete).toHaveBeenCalledTimes(1)
  })

  it('显示进度「第 N / 6 爻」', () => {
    render(<CastingStage onComplete={vi.fn()} />)
    expect(screen.getByText(/\/ 6 爻/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- CastingStage`
Expected: FAIL

- [ ] **Step 3: 实现 CoinToss**

Create `src/components/CoinToss.tsx`:
```tsx
import { motion } from 'framer-motion'

/** 三枚铜钱的抛掷视觉（纯展示）。spinning 时旋转上抛。 */
export function CoinToss({ spinning }: { spinning: boolean }) {
  return (
    <div className="flex gap-3">
      {[0, 1, 2].map((i) => (
        <motion.div
          key={i}
          className="w-8 h-8 rounded-full border border-gold flex items-center justify-center"
          style={{ boxShadow: '0 0 10px rgba(230,191,114,.3)' }}
          animate={spinning ? { y: [-12, 0], rotateY: [0, 360] } : { y: 0 }}
          transition={{ duration: 0.5, delay: i * 0.08, repeat: spinning ? Infinity : 0 }}
        >
          <span className="block w-2 h-2 border border-gold" />
        </motion.div>
      ))}
    </div>
  )
}
```

- [ ] **Step 4: 实现 CastingStage**

Create `src/components/CastingStage.tsx`:
```tsx
import { useEffect, useRef, useState } from 'react'
import { CoinToss } from './CoinToss'

interface Props {
  onComplete: () => void
  /** 每掷间隔（ms），测试可传 0 */
  throwInterval?: number
}

const TOTAL = 6

export function CastingStage({ onComplete, throwInterval = 900 }: Props) {
  const [thrown, setThrown] = useState(0)
  const done = useRef(false)

  const finish = () => {
    if (done.current) return
    done.current = true
    onComplete()
  }

  useEffect(() => {
    if (thrown >= TOTAL) {
      finish()
      return
    }
    const t = setTimeout(() => setThrown((n) => n + 1), throwInterval)
    return () => clearTimeout(t)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [thrown, throwInterval])

  return (
    <div className="flex flex-col items-center gap-8">
      <p className="text-xs tracking-widest text-cinnabar font-serif">第 {Math.min(thrown + 1, TOTAL)} / {TOTAL} 爻</p>
      <CoinToss spinning={thrown < TOTAL} />
      <button className="text-xs text-paper/40 underline font-serif" onClick={finish}>
        跳过
      </button>
    </div>
  )
}
```

- [ ] **Step 5: 运行，确认通过**

Run: `npm run test -- CastingStage`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/components/CoinToss.tsx src/components/CastingStage.tsx src/components/CastingStage.test.tsx
git commit -m "feat: 摇卦动画编排（CoinToss + CastingStage）"
```

---

### Task 11: ResultView 与 ShareCard

**Files:**
- Create: `src/components/ResultView.tsx`, `src/components/ShareCard.tsx`
- Test: `src/components/ResultView.test.tsx`, `src/components/ShareCard.test.tsx`

- [ ] **Step 1: 写失败测试（ShareCard）**

Create `src/components/ShareCard.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { ShareCard } from './ShareCard'
import { Interpretation } from '../domain/interpret'
import { Hexagram as Hex } from '../domain/types'

const lines: Hex = Array.from({ length: 6 }, () => ({ yinyang: 'yang', moving: false })) as Hex
const interp: Interpretation = {
  question: '我该换工作吗？',
  primaryName: '乾为天',
  changedName: null,
  judgment: '乾：元，亨，利，贞。',
  movingLineTexts: [],
}

describe('ShareCard', () => {
  it('含所问之事与卦名', () => {
    render(<ShareCard interpretation={interp} lines={lines} shiYao={6} yingYao={3} dateText="2026.06.16" />)
    expect(screen.getByText('我该换工作吗？')).toBeInTheDocument()
    expect(screen.getByText(/乾为天/)).toBeInTheDocument()
  })
  it('含站点水印', () => {
    render(<ShareCard interpretation={interp} lines={lines} shiYao={6} yingYao={3} dateText="2026.06.16" />)
    expect(screen.getByText(/六爻占/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- ShareCard`
Expected: FAIL

- [ ] **Step 3: 实现 ShareCard**

Create `src/components/ShareCard.tsx`:
```tsx
import { forwardRef } from 'react'
import { Hexagram } from './Hexagram'
import { Hexagram as Hex } from '../domain/types'
import { Interpretation } from '../domain/interpret'

interface Props {
  interpretation: Interpretation
  lines: Hex
  shiYao: number
  yingYao: number
  dateText: string
}

/** 分享卡片。离屏渲染后由 useShareImage 栅格化为 PNG。 */
export const ShareCard = forwardRef<HTMLDivElement, Props>(function ShareCard(
  { interpretation, lines, shiYao, yingYao, dateText },
  ref,
) {
  return (
    <div
      ref={ref}
      className="w-[360px] flex flex-col items-center gap-4 p-8 font-serif"
      style={{ background: 'radial-gradient(circle at 50% 28%, #15110b, #080604)' }}
    >
      <div className="text-[10px] tracking-[0.3em] text-cinnabar">六爻 · 周易</div>
      <div className="text-[10px] tracking-widest text-cinnabar">所 问</div>
      <div className="text-paper text-center">{interpretation.question}</div>
      <div className="text-xl text-gold-bright text-glow-gold">{interpretation.primaryName}</div>
      <Hexagram lines={lines} shiYao={shiYao} yingYao={yingYao} />
      <div className="text-xs text-paper/70 text-center">{interpretation.judgment}</div>
      <div className="text-[9px] text-paper/40">{dateText} · 六爻占</div>
    </div>
  )
})
```

- [ ] **Step 4: 写失败测试（ResultView）**

Create `src/components/ResultView.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { ResultView } from './ResultView'
import { CastReading } from '../domain/reading'
import { Interpretation } from '../domain/interpret'
import { Hexagram as Hex } from '../domain/types'

const lines: Hex = Array.from({ length: 6 }, () => ({ yinyang: 'yang', moving: true })) as Hex
const reading = {
  question: '问', 
  primary: { lines, data: { name: '乾为天', shiYao: 6, yingYao: 3 } },
  changed: { lines, data: { name: '坤为地', shiYao: 6, yingYao: 3 } },
  movingIndexes: [0, 1, 2, 3, 4, 5],
} as unknown as CastReading
const interp: Interpretation = {
  question: '问', primaryName: '乾为天', changedName: '坤为地',
  judgment: '乾：元，亨，利，贞。',
  movingLineTexts: [{ index: 0, text: '初九：潜龙勿用。' }],
}

describe('ResultView', () => {
  it('显示卦名、卦辞、动爻爻辞', () => {
    render(<ResultView reading={reading} interpretation={interp} onShare={vi.fn()} />)
    expect(screen.getByText(/乾为天/)).toBeInTheDocument()
    expect(screen.getByText(/元，亨，利，贞/)).toBeInTheDocument()
    expect(screen.getByText(/潜龙勿用/)).toBeInTheDocument()
  })
  it('有变卦时显示变卦名', () => {
    render(<ResultView reading={reading} interpretation={interp} onShare={vi.fn()} />)
    expect(screen.getByText(/坤为地/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 5: 运行，确认失败**

Run: `npm run test -- ResultView`
Expected: FAIL

- [ ] **Step 6: 实现 ResultView**

Create `src/components/ResultView.tsx`:
```tsx
import { Hexagram } from './Hexagram'
import { CastReading } from '../domain/reading'
import { Interpretation } from '../domain/interpret'

interface Props {
  reading: CastReading
  interpretation: Interpretation
  onShare: () => void
}

export function ResultView({ reading, interpretation, onShare }: Props) {
  const { primary, changed } = reading
  return (
    <div className="flex flex-col items-center gap-6 px-6 w-full max-w-md mx-auto font-serif">
      <div className="text-xl text-gold-bright text-glow-gold">{primary.data.name}</div>
      <div className="flex items-center gap-6">
        <Hexagram lines={primary.lines} shiYao={primary.data.shiYao} yingYao={primary.data.yingYao} />
        {changed && (
          <>
            <span className="text-paper/40">→</span>
            <div className="opacity-50">
              <Hexagram lines={changed.lines} shiYao={changed.data.shiYao} yingYao={changed.data.yingYao} />
            </div>
          </>
        )}
      </div>
      {changed && <div className="text-xs text-paper/60">变卦 · {changed.data.name}</div>}
      <div className="text-sm text-paper/80 text-center"><span className="text-gold-bright">卦辞　</span>{interpretation.judgment}</div>
      {interpretation.movingLineTexts.map((m) => (
        <div key={m.index} className="text-xs text-paper/70 text-center">
          <span className="inline-block h-2 w-2 rounded-full bg-cinnabar mr-2 align-middle" />
          {m.text}
        </div>
      ))}
      <button className="mt-2 font-serif tracking-[0.3em] text-gold-bright border border-gold/60 rounded-full px-6 py-2" onClick={onShare}>
        生 成 分 享 图
      </button>
    </div>
  )
}
```

- [ ] **Step 7: 运行，确认通过**

Run: `npm run test -- ResultView ShareCard`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add src/components/ResultView.tsx src/components/ShareCard.tsx src/components/ResultView.test.tsx src/components/ShareCard.test.tsx
git commit -m "feat: ResultView 出卦页与 ShareCard 分享卡"
```

---

## Phase 4 — 分享图与状态机

### Task 12: useShareImage（DOM→PNG）

**Files:**
- Create: `src/hooks/useShareImage.ts`
- Test: `src/hooks/useShareImage.test.ts`

> modern-screenshot 与 navigator.share 在测试中被 mock；只验证「字体就绪→栅格化→优先分享，失败兜底下载」的编排。

- [ ] **Step 1: 写失败测试**

Create `src/hooks/useShareImage.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'

const domToPng = vi.fn(async () => 'data:image/png;base64,AAAA')
vi.mock('modern-screenshot', () => ({ domToPng: (...a: unknown[]) => domToPng(...a) }))

import { useShareImage } from './useShareImage'

beforeEach(() => {
  domToPng.mockClear()
  // @ts-expect-error 测试桩
  global.document.fonts = { ready: Promise.resolve() }
})

describe('useShareImage', () => {
  it('生成 PNG 并在不支持分享时返回 dataUrl（下载兜底）', async () => {
    // @ts-expect-error 模拟不支持 share
    global.navigator.share = undefined
    const { result } = renderHook(() => useShareImage())
    const node = document.createElement('div')
    let url = ''
    await act(async () => { url = await result.current.capture(node) })
    expect(domToPng).toHaveBeenCalledTimes(1)
    expect(url).toContain('data:image/png')
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- useShareImage`
Expected: FAIL

- [ ] **Step 3: 实现**

Create `src/hooks/useShareImage.ts`:
```ts
import { useCallback } from 'react'
import { domToPng } from 'modern-screenshot'

export function useShareImage() {
  const capture = useCallback(async (node: HTMLElement): Promise<string> => {
    // 截图前确保衬线字体就绪，避免回退字体
    if (document.fonts?.ready) await document.fonts.ready
    const dataUrl = await domToPng(node, { scale: 3 })

    // 优先原生分享（移动端）
    try {
      if (navigator.share && navigator.canShare) {
        const blob = await (await fetch(dataUrl)).blob()
        const file = new File([blob], 'liuyao.png', { type: 'image/png' })
        if (navigator.canShare({ files: [file] })) {
          await navigator.share({ files: [file], title: '六爻占' })
        }
      }
    } catch {
      // 用户取消或不支持，回退到 dataUrl 由调用方提供下载
    }
    return dataUrl
  }, [])

  return { capture }
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- useShareImage`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useShareImage.ts src/hooks/useShareImage.test.ts
git commit -m "feat: useShareImage（字体就绪→DOM转PNG→分享/下载）"
```

---

### Task 13: useCasting 状态机 + App 装配

**Files:**
- Create: `src/hooks/useCasting.ts`
- Modify: `src/App.tsx`
- Test: `src/hooks/useCasting.test.ts`, `src/App.test.tsx`

- [ ] **Step 1: 写失败测试（useCasting）**

Create `src/hooks/useCasting.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { useCasting } from './useCasting'
import { sequenceRandom } from '../domain/random'

describe('useCasting 状态机', () => {
  it('初始为 input', () => {
    const { result } = renderHook(() => useCasting(sequenceRandom([0, 0, 0])))
    expect(result.current.phase).toBe('input')
  })
  it('submit → casting；finishCasting → result，并产出解读', async () => {
    const { result } = renderHook(() => useCasting(sequenceRandom([0, 0, 0])))
    act(() => result.current.submit('我该换工作吗？'))
    expect(result.current.phase).toBe('casting')
    await act(async () => { await result.current.finishCasting() })
    expect(result.current.phase).toBe('result')
    expect(result.current.interpretation?.primaryName).toContain('乾为天')
    expect(result.current.reading?.question).toBe('我该换工作吗？')
  })
})
```

- [ ] **Step 2: 运行，确认失败**

Run: `npm run test -- useCasting`
Expected: FAIL

- [ ] **Step 3: 实现 useCasting**

Create `src/hooks/useCasting.ts`:
```ts
import { useCallback, useState } from 'react'
import { RandomSource, cryptoRandom } from '../domain/random'
import { buildReading, CastReading } from '../domain/reading'
import { interpret, Interpretation } from '../domain/interpret'

type Phase = 'input' | 'casting' | 'result'

export function useCasting(rng: RandomSource = cryptoRandom) {
  const [phase, setPhase] = useState<Phase>('input')
  const [question, setQuestion] = useState('')
  const [reading, setReading] = useState<CastReading | null>(null)
  const [interpretation, setInterpretation] = useState<Interpretation | null>(null)

  const submit = useCallback((q: string) => {
    setQuestion(q)
    setPhase('casting')
  }, [])

  const finishCasting = useCallback(async () => {
    const r = buildReading(question, rng)
    setReading(r)
    setInterpretation(await interpret(r))
    setPhase('result')
  }, [question, rng])

  const reset = useCallback(() => {
    setReading(null)
    setInterpretation(null)
    setQuestion('')
    setPhase('input')
  }, [])

  return { phase, reading, interpretation, submit, finishCasting, reset }
}
```

- [ ] **Step 4: 运行，确认通过**

Run: `npm run test -- useCasting`
Expected: PASS

- [ ] **Step 5: 写失败测试（App 集成）**

Create `src/App.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import App from './App'

describe('App 全流程', () => {
  it('输入→摇卦(跳过)→出卦', async () => {
    render(<App />)
    await userEvent.type(screen.getByRole('textbox'), '我该换工作吗？')
    await userEvent.click(screen.getByRole('button', { name: /摇卦/ }))
    await userEvent.click(screen.getByRole('button', { name: /跳过/ }))
    // 出卦页出现卦名（任意卦）与分享按钮
    expect(await screen.findByRole('button', { name: /分享/ })).toBeInTheDocument()
  })
})
```

- [ ] **Step 6: 运行，确认失败**

Run: `npm run test -- App`
Expected: FAIL

- [ ] **Step 7: 实现 App**

Replace `src/App.tsx`:
```tsx
import { useRef } from 'react'
import { useCasting } from './hooks/useCasting'
import { useShareImage } from './hooks/useShareImage'
import { QuestionInput } from './components/QuestionInput'
import { CastingStage } from './components/CastingStage'
import { ResultView } from './components/ResultView'
import { ShareCard } from './components/ShareCard'

export default function App() {
  const { phase, reading, interpretation, submit, finishCasting, reset } = useCasting()
  const { capture } = useShareImage()
  const cardRef = useRef<HTMLDivElement>(null)

  const handleShare = async () => {
    if (!cardRef.current) return
    const url = await capture(cardRef.current)
    const a = document.createElement('a')
    a.href = url
    a.download = '六爻占.png'
    a.click()
  }

  const dateText = new Date().toISOString().slice(0, 10).replace(/-/g, '.')

  return (
    <main className="min-h-screen flex flex-col items-center justify-center py-12">
      {phase === 'input' && <QuestionInput onSubmit={submit} />}
      {phase === 'casting' && <CastingStage onComplete={finishCasting} />}
      {phase === 'result' && reading && interpretation && (
        <>
          <ResultView reading={reading} interpretation={interpretation} onShare={handleShare} />
          <button className="mt-8 text-xs text-paper/40 underline font-serif" onClick={reset}>再 占 一 卦</button>
          {/* 离屏分享卡，供栅格化 */}
          <div className="fixed -left-[9999px] top-0" aria-hidden>
            <ShareCard
              ref={cardRef}
              interpretation={interpretation}
              lines={reading.primary.lines}
              shiYao={reading.primary.data.shiYao}
              yingYao={reading.primary.data.yingYao}
              dateText={dateText}
            />
          </div>
        </>
      )}
    </main>
  )
}
```

- [ ] **Step 8: 运行，确认通过**

Run: `npm run test -- App`
Expected: PASS

- [ ] **Step 9: 全量测试 + 覆盖率**

Run: `npm run test -- --coverage`
Expected: 全绿，覆盖率 ≥ 80%。若某文件偏低，补测试。

- [ ] **Step 10: Commit**

```bash
git add src/hooks/useCasting.ts src/App.tsx src/hooks/useCasting.test.ts src/App.test.tsx
git commit -m "feat: 状态机 useCasting 与 App 全流程装配"
```

---

## Phase 5 — 字体、E2E、部署

### Task 14: 子集化衬线字体

**Files:**
- Create: `src/styles/fonts.css`, `public/fonts/`（子集字体文件）
- Modify: `src/main.tsx`（import fonts.css）

- [ ] **Step 1: 生成子集字体**

收集站内用到的全部汉字（64 卦名 + 卦辞 + 爻辞 + UI 文案），用子集化工具（如 `fonttools pyftsubset` 或 `subset-font`）从一款可商用宋体/明朝体生成 woff2，放入 `public/fonts/`。
Run（示例）:
```bash
pyftsubset SourceHanSerif.otf --text-file=chars.txt --flavor=woff2 --output-file=public/fonts/liuyao-serif.woff2
```

- [ ] **Step 2: 声明 @font-face**

Create `src/styles/fonts.css`:
```css
@font-face {
  font-family: 'LiuyaoSerif';
  src: url('/liuyao/fonts/liuyao-serif.woff2') format('woff2');
  font-display: swap;
}
```
并在 `tailwind.config.ts` 的 `fontFamily.serif` 最前面加入 `'LiuyaoSerif'`。在 `src/main.tsx` `import './styles/fonts.css'`。

- [ ] **Step 3: 验证**

Run: `npm run dev`，打开页面确认卦名/卦辞为衬线字；`npm run build` 成功。

- [ ] **Step 4: Commit**

```bash
git add public/fonts src/styles/fonts.css src/main.tsx tailwind.config.ts
git commit -m "feat: 子集化衬线字体，保证分享图字形一致"
```

---

### Task 15: Playwright E2E happy path

**Files:**
- Create: `playwright.config.ts`, `tests/e2e/divination.spec.ts`

- [ ] **Step 1: 配置 Playwright**

Create `playwright.config.ts`:
```ts
import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  webServer: { command: 'npm run dev', url: 'http://localhost:5173/liuyao/', reuseExistingServer: true },
  use: { baseURL: 'http://localhost:5173/liuyao/' },
})
```
Run: `npx playwright install --with-deps chromium`

- [ ] **Step 2: 写 E2E**

Create `tests/e2e/divination.spec.ts`:
```ts
import { test, expect } from '@playwright/test'

test('完整占卦流程到出卦', async ({ page }) => {
  await page.goto('/')
  await expect(page.getByText('心有所问')).toBeVisible()
  await page.getByRole('textbox').fill('我该换工作吗？')
  await page.getByRole('button', { name: /摇卦/ }).click()
  await page.getByRole('button', { name: /跳过/ }).click()
  await expect(page.getByText('卦辞')).toBeVisible()
  await expect(page.getByRole('button', { name: /分享/ })).toBeVisible()
})
```

- [ ] **Step 3: 运行**

Run: `npm run e2e`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add playwright.config.ts tests/e2e/divination.spec.ts
git commit -m "test: Playwright E2E 占卦 happy path"
```

---

### Task 16: GitHub Actions 部署到 Pages

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: 写 workflow**

Create `.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npm run test
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: 校验 base 与仓库名一致**

确认 `vite.config.ts` 的 `base` 与实际仓库名一致（若仓库非 `liuyao`，同步改 `base`、`fonts.css` 路径、`playwright.config.ts` 的 URL）。

- [ ] **Step 3: 本地最终验证**

Run: `npm run test && npm run build`
Expected: 全绿、构建成功。

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: GitHub Actions 构建并部署到 GitHub Pages"
```

- [ ] **Step 5: 部署说明（人工）**

合并到 `main` 后，在 GitHub 仓库 Settings → Pages 选择 Source = GitHub Actions。首次 push 后 Actions 自动构建部署；访问 `https://<user>.github.io/<repo>/`。

---

## Self-Review（已对照 spec 核查）

- **Spec 覆盖**：① 输入所问→Task 9；② 摇卦动画→Task 10；③ 出卦(卦名/六爻/动爻/世应/卦辞/动爻爻辞=甲案)→Task 8/11；④ 分享卡(含所问)→Task 11/12。起卦 6/7/8/9 规则→Task 1/3；变卦→Task 3；世应→Task 5/8；查卦→Task 6；interpret 接缝(async)→Task 7；数据集+完整性→Task 5；墨金 tokens→Task 0；DOM→PNG/字体就绪→Task 12/14；必填→Task 9；部署 base+Actions→Task 0/16；测试 ≥80% 单元/集成/E2E→各 Task + Task 13/15。
- **二期 AI 接缝**：interpret 签名 async，前端仅依赖 `Interpretation`，替换实现即可。
- **占位符**：无 TODO/TBD；数据录入(Task 5)与字体子集(Task 14)为明确的、由测试/清单守护的产出步骤。
- **类型一致**：`Line`/`Hexagram`/`CastReading`/`HexagramResult`/`Interpretation`/`RandomSource` 跨任务签名一致；`buildReading`/`interpret`/`lookupHexagram`/`capture` 命名前后统一。
